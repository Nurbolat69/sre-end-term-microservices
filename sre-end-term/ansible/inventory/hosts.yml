---
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: auto
          project_root: "{{ playbook_dir }}/../.."
          sre_root: "{{ playbook_dir }}/.."

    swarm_managers:
      hosts: {}
      # Example after Terraform AWS apply:
      # swarm-1:
      #   ansible_host: 54.123.45.67
      #   ansible_user: ubuntu
      #   ansible_ssh_private_key_file: ~/.ssh/sre-demo.pem

    k8s_workers:
      hosts: {}
