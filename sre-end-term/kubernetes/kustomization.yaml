apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sock-shop

resources:
  - 00-namespace.yaml
  - 01-hpa-orders-payment.yaml
  - 02-orders-podmonitor.yaml
  - 03-pdb-orders.yaml
  - 04-configmap-deploy-notes.yaml
# Monitoring namespace rules: kubectl apply -f ../monitoring/prometheus-slo-alerts.yaml

# Core workloads from upstream manifests:
#   kubectl apply -f ../../deploy/kubernetes/manifests/
#   kubectl apply -f ../../deploy/kubernetes/manifests-monitoring/

commonLabels:
  project: sre-end-term
  managed-by: kustomize
